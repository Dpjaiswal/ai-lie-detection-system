# ==============================================================
# AI-Powered Lie Detection System - Requirements
# ==============================================================
#
# INSTALL ORDER:
#   pip install -r requirements.txt          <- core (API + Dashboard)
#   pip install -r requirements-ml.txt       <- ML training (heavy)
#   pip install -r requirements-optional.txt <- XAI, cloud, DB extras
#
# ==============================================================

# --- Core Scientific ------------------------------------------
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.2

# --- NLP (lightweight) ----------------------------------------
nltk>=3.8.1
# NOTE: After install run:
#   python -m nltk.downloader punkt stopwords wordnet

# --- Audio Processing -----------------------------------------
librosa>=0.10.1
soundfile>=0.12.1
audioread>=3.0.0
# sounddevice - optional, for microphone recording
# pyaudio     - optional, requires: brew install portaudio
#               then: pip install pyaudio
# webrtcvad   - optional, for voice activity detection
# praat-parselmouth - optional, for jitter/shimmer (brew install praat)

# --- Visualization --------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.17.0

# --- FastAPI Backend ------------------------------------------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6
httpx>=0.25.0
websockets>=12.0
aiofiles>=23.2.1

# --- Streamlit Dashboard --------------------------------------
streamlit>=1.28.0
# streamlit-audiorecorder - optional (requires pyaudio -> brew install portaudio first)
# streamlit-webrtc       - optional (WebRTC microphone support)

# --- Utilities ------------------------------------------------
python-dotenv>=1.0.0
pyyaml>=6.0.1
click>=8.1.7
rich>=13.6.0
tqdm>=4.66.1
loguru>=0.7.2

# --- Testing --------------------------------------------------
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
faker>=20.0.0

# --- Code Quality ---------------------------------------------
black>=23.10.0
isort>=5.12.0
flake8>=6.1.0
